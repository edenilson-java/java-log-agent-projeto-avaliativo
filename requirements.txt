# Orquestração do fluxo agêntico
langgraph==1.2.9
langchain-core==1.4.9
langchain-openai==1.3.5

# Contratos, configuração tipada e validação de fronteira
pydantic==2.13.4

# Carga de variáveis de ambiente a partir do .env local
python-dotenv==1.0.1

# API local e cliente HTTP exigido pelo TestClient do FastAPI
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.28.1

# Servidor MCP local, transporte stdio
mcp==2.0.0

# Suíte automatizada e lint
pytest==9.1.1
ruff==0.16.4

# Parse do workflow de CI nos testes
PyYAML==6.0.3
